services:
  - type: web
    name: scoreboard-crisma
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: AUTH_USER
        value: crisma
      - key: AUTH_PASSWORD
        sync: false
